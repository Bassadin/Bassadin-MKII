# Repo Instructions

## package.json

- Pin dependency versions to exact versions (no `^` or `~` ranges).

## Versioning and commits

- Use Semantic Versioning (semver) for releases.
- Use Conventional Commits for commit messages.
